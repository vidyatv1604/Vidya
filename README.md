
The First.java file is for printing hello world while we run the file.

The ArrayListExp.java file is for understanding ArrayList and its basic operatins. First we created an arraylist and then added some elements to it, the removed the some elements that we added before. ArrayList is very flexible and easy to use because of the dynamic nature and the inbuild functions that support manage the elements that are in it.

LinkedListExp.java In this java file we learn about the basics about LinkedList. We added 4 elements using .add() function. This was displayed using a for loop. Then the remove operation is shown. After that addFirst() is shown in the code.
